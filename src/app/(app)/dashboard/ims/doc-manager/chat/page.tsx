import { ImsAgentWorkspace } from "@/components/ims/ims-agent-workspace";

export default function DocManagerChatPage() {
  return (
    <ImsAgentWorkspace
      heading="Doc Manager Agent"
      description="Use the conversational workspace to draft, revise, and issue controlled IMS documents."
    />
  );
}
